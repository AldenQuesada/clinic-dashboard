{
  "name": "clinic-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit -p tsconfig.checkjs.json",
    "audit:schema": "node scripts/audit-schema.cjs",
    "perf:budget": "node scripts/perf-budget.cjs",
    "cache:bust": "node scripts/cache-bust.cjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.9"
  }
}
